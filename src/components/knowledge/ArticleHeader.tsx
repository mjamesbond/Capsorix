import type { KnowledgeArticle } from "@/knowledge/schema";
import CanonSequence from "./CanonSequence";
export default function ArticleHeader({article,total}:{article:KnowledgeArticle;total:number}) { return <header className="mb-12 border-b border-border pb-10"><CanonSequence order={article.order} total={total}/>{!article.hasBodyH1&&<h1 className="mt-5 font-display text-4xl md:text-6xl">{article.title}</h1>}<p className="mt-5 max-w-3xl text-lg leading-relaxed text-muted-foreground">{article.description}</p><p className="mt-5 text-sm text-muted-foreground">{article.readingMinutes} min read</p>{article.concepts.length>0&&<div className="mt-6 flex flex-wrap gap-2" aria-label="Concepts">{article.concepts.map(x=><span key={x} className="rounded-full border border-border px-3 py-1 text-xs">{x}</span>)}</div>}{article.methods.length>0&&<div className="mt-3 flex flex-wrap gap-2" aria-label="Methods">{article.methods.map(x=><span key={x} className="rounded-full bg-muted px-3 py-1 text-xs">Method · {x}</span>)}</div>}</header>; }
