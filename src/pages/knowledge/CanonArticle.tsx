import { Navigate,useParams } from "react-router-dom";import KnowledgeShell from "@/components/knowledge/KnowledgeShell";import ArticleHeader from "@/components/knowledge/ArticleHeader";import ArticleBody from "@/components/knowledge/ArticleBody";import TableOfContents from "@/components/knowledge/TableOfContents";import ArticleNavigation from "@/components/knowledge/ArticleNavigation";import {articles,collection} from "@/knowledge/manifest";
export default function CanonArticle(){const {slug}=useParams();const article=articles.find(x=>x.slug===slug);if(!article)return <Navigate to="/knowledge/canon" replace/>;return <KnowledgeShell><article className="container py-16"><ArticleHeader article={article} total={collection.totalSize}/><div className="grid gap-12 lg:grid-cols-[minmax(0,1fr)_18rem]"><ArticleBody html={article.html}/><aside className="lg:order-last"><TableOfContents entries={article.toc}/></aside></div><ArticleNavigation article={article} articles={articles}/></article></KnowledgeShell>}
